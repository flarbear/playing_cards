name: playing_cards
description: A Flutter project with classes for expressing a Playing Card and rendering it.
version: 0.0.1
author: flarbear
homepage: http://github.com/flarbear

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
